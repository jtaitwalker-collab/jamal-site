import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Jamal Walker — Senior Account Executive",
  description:
    "Enterprise SaaS sales leader. Ask me anything via voice or text.",
  openGraph: {
    title: "Jamal Walker — Senior Account Executive",
    description:
      "9+ years closing six-figure ARR deals across AI, data analytics, cybersecurity and fintech SaaS.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body>{children}</body>
    </html>
  );
}
