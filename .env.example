# Server-side only — never exposed to the browser
ANTHROPIC_API_KEY=sk-ant-...

# Client-side — safe to expose; restrict to your domain in Deepgram dashboard
NEXT_PUBLIC_DEEPGRAM_API_KEY=your_deepgram_key_here
